% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{registry}
\alias{registry}
\title{Declare a package's external resources}
\usage{
registry(
  package,
  resources,
  remote = NULL,
  policy = c("bundled", "current", "pinned", "offline"),
  current = NULL
)
}
\arguments{
\item{package}{Name of the declaring package. Becomes part of every
resource identity and scopes the cache, so two packages declaring the
same resource name never collide.}

\item{resources}{A list of \code{\link[=resource]{resource()}} records.}

\item{remote}{Optional URL of an author-controlled registry file. Consulted
only under the \code{"current"} policy. It may repair or add mirrors and may
introduce new versions. It may never change the bytes a published version
refers to.}

\item{policy}{Default resolution policy for this package. One of
\code{"bundled"}, \code{"current"}, \code{"pinned"}, \code{"offline"}. See \code{\link[=getaca_policy]{getaca_policy()}}.}

\item{current}{Named character vector giving the channel head: the version a
bare request for each resource name resolves to, as
\code{c(wfo = "2026-09")}. Required for any name declaring more than one
version, and optional for the rest, since a name with one version has only
one answer.}
}
\value{
An object of class \code{getaca_registry}.
}
\description{
A registry is one package's declaration of what it needs. It carries no
download logic: getaca is the single engine, and every package supplies
only its own list of resource records.
}
\details{
Ship the result at \code{inst/getaca/registry.rds} via \code{\link[=registry_write]{registry_write()}}.
getaca discovers it with \code{system.file()}, so no registration call and no
load hook are required.
}
\section{Identity}{

A registry state is identified by \code{\link[=registry_digest]{registry_digest()}}, derived from the
declaration itself, and recorded in the provenance of every resource it
resolves. There is no revision number to keep in step: a digest cannot be
typed wrong, and two states that differ cannot claim to be the same one.
\code{\link[=registry_write]{registry_write()}} stamps \code{created}, which is what orders two states in
time, and a bundled registry additionally has the version of the package
that ships it.
}

\section{Channel heads}{

A registry declares records; a channel points at one of them. When a
resource name carries several versions, which of them \code{getaca("name")}
returns is a decision, so the registry states it in \code{current} rather than
leaving it to declaration order. A registry that declares two versions of a
name without naming a head is refused, which is what stops a version
appended in the wrong place from silently moving every user backwards.
}

\examples{
registry(
  package = "mypackage",
  resources = list(
    resource("reference-data", "2.1",
             urls = "https://example.org/ref-2.1.zip",
             sha256 = strrep("b", 64))
  )
)

# Two versions on offer, one of them the channel head:
registry(
  package = "mypackage",
  current = c("reference-data" = "2.1"),
  resources = list(
    resource("reference-data", "2.0",
             urls = "https://example.org/ref-2.0.zip",
             sha256 = strrep("a", 64)),
    resource("reference-data", "2.1",
             urls = "https://example.org/ref-2.1.zip",
             sha256 = strrep("b", 64))
  )
)
}
