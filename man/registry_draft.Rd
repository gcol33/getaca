% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draft.R
\name{registry_draft}
\alias{registry_draft}
\title{Draft a registry from where the data is}
\usage{
registry_draft(
  x,
  package,
  version = NULL,
  source = "auto",
  keep = FALSE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{x}{Locations. A character vector, where each element is one location,
or a list, where each element is a character vector of mirrors for one
resource. Names, if any, name the resources.}

\item{package}{Declaring package name.}

\item{version}{Version label for every resource drafted. Optional where the
archive supplies one, required for a plain URL.}

\item{source}{Which handler to use: \code{"auto"}, or one of \code{"zenodo"},
\code{"figshare"}, \code{"dataverse"}, \code{"url"} to override the detection.}

\item{keep}{Keep the downloaded bytes in the cache, so that a later
\code{\link[=getaca]{getaca()}} call for the drafted resource finds them already there instead
of transferring them a second time.}

\item{quiet}{Suppress transfer progress.}

\item{...}{Passed to \code{\link[=registry]{registry()}}, for \code{remote}, \code{policy}, \code{keys} and \code{auth}.}
}
\value{
A \code{getaca_registry}.
}
\description{
Takes locations, returns a \code{\link[=registry]{registry()}} with every checksum filled in. What
it saves is the part of authoring that cannot be done by hand: a SHA-256 has
to be computed from the bytes, which means retrieving them.
}
\details{
A location is a plain URL, or an identifier for a data archive that holds
several files. Which one it is, and which archive, is read off the string, so
one call covers both:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{registry_draft("10.5281/zenodo.17844561", package = "mypackage")
registry_draft(c(wfo = "https://example.org/wfo.parquet"),
               package = "mypackage", version = "2026.1")
}\if{html}{\out{</div>}}

Every file is downloaded once and hashed locally. Checksums an archive
reports are not used: they are md5 at all three archives supported here, and
they arrive from the host that serves the bytes, so they say nothing the
transfer itself has not already said.

This is an authoring tool. Nothing in the retrieval path calls it, and a
drafted registry names ordinary \verb{https://} locations, so an archive is
consulted when the registry is written and never when a user fetches.
}
\section{Archives}{

\describe{
\item{Zenodo}{\code{"10.5281/zenodo.17844561"} or a
\verb{https://zenodo.org/records/...} URL. Version defaults to the record id,
which Zenodo mints afresh for each version.}
\item{figshare}{\code{"10.6084/m9.figshare.14763051.v1"} or a
\verb{https://figshare.com/articles/...} URL. A DOI without a \code{.vN} suffix
resolves to whatever figshare currently calls latest, and the version it
served is what the draft records.}
\item{Dataverse}{A dataset DOI, or a
\verb{https://<host>/dataset.xhtml?persistentId=...} URL. Instances are
self-hosted under their own DOI prefixes, so a bare DOI is resolved
through \code{doi.org} to find which host to ask. The other two are
recognised from the string alone and cost no such lookup.}
}
}

\section{What to edit afterwards}{

A draft is a starting point. Resources are named after their files, which is
rarely the name you want a user to type, and \code{description} is left empty.
Both are ordinary arguments of \code{\link[=resource]{resource()}}; edit the call, or edit the
returned registry, and write it with \code{\link[=registry_write]{registry_write()}}.
}

\examples{
\dontrun{
reg <- registry_draft("10.5281/zenodo.17844561", package = "mypackage")
registry_write(reg, "inst/getaca/registry.rds")
}
}
\seealso{
\code{\link[=registry_write]{registry_write()}} to ship it, \code{\link[=registry_sign]{registry_sign()}} to sign it.
}
