% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{getaca_info}
\alias{getaca_info}
\title{Provenance for a resource}
\usage{
getaca_info(
  name,
  package = NULL,
  registry = NULL,
  version = NULL,
  processed = TRUE
)
}
\arguments{
\item{name}{Resource name as declared by \code{package}.}

\item{package}{Declaring package. The resource identity is
\code{package / name / version}, so two packages declaring the same name never
collide.}

\item{registry}{A \code{\link[=registry]{registry()}} object, for standalone use without a
declaring package.}

\item{version}{Explicit version, bypassing channel resolution. Use this to
hold an analysis to one release.}

\item{processed}{Apply the declared \code{\link[=processor]{processor()}}, when there is one, and
return the processed path. \code{FALSE} returns the raw artefact.}
}
\value{
A \code{getaca_entry}, or \code{NULL} when the resource is not cached.
}
\description{
Answers, for a cached resource: which package declared it, which registry
state and which policy resolved it, the exact version, declared and observed
checksums, which mirror served it, when it was fetched and when it was last
fully verified, its license, any processor applied, which getaca retrieved
it, and the local path. Suitable for a reproducibility appendix or a bug
report.
}
\details{
The registry state appears as a \code{\link[=registry_digest]{registry_digest()}}, so the declaration that
resolved the resource can be identified exactly rather than by a number
someone kept in step by hand.
}
