% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{reporter}
\alias{reporter}
\title{Write a progress reporter}
\usage{
reporter(id, fn)
}
\arguments{
\item{id}{Short stable identifier, for example \code{"bar"} or \code{"shiny"}.}

\item{fn}{A function of one argument, the event. Its return value is
ignored.}
}
\value{
An object of class \code{getaca_reporter}.
}
\description{
A reporter turns transfer events into whatever you want a transfer to look
like: a bar, a log line, a row in a database, an update to a Shiny session.
It carries an \code{id} so that what is reporting is visible when one is set.
}
\details{
See \link{getaca-progress} for the events and their fields.
}
\examples{
# Only the totals, once each transfer is done.
reporter("totals", function(event) {
  if (identical(event$type, "end")) {
    cat(format(event$id), event$status, event$bytes, "\n")
  }
})
}
\seealso{
\code{\link[=getaca_progress]{getaca_progress()}}
}
