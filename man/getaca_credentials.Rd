% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{getaca_credentials}
\alias{getaca_credentials}
\title{Which credentials a package expects}
\usage{
getaca_credentials(package = NULL, registry = NULL)
}
\arguments{
\item{package}{Declaring package. Ignored when \code{registry} is supplied.}

\item{registry}{A \code{\link[=registry]{registry()}} object, for standalone use.}
}
\value{
A data frame with one row per declared variable: \code{package}, \code{host},
\code{scheme}, \code{variable}, \code{set} and \code{register}. Empty when nothing is declared.
}
\description{
Reports the environment variables a package's declaration reads, and whether
each is set in this session. Answers "do I have what I need" before a fetch
rather than during one, without touching the network.
}
\details{
Values are never read or shown. \code{set} says only that the variable holds
something.
}
\examples{
reg <- registry("demo",
  auth = list(auth_host("data.example.org", bearer("EXAMPLE_TOKEN"))),
  resources = list(
    resource("example", "1.0",
             urls = "https://data.example.org/example-1.0.csv",
             sha256 = strrep("c", 64))
  ))
getaca_credentials(registry = reg)
}
\seealso{
\link{getaca-auth}
}
