% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature.R
\name{registry_keygen}
\alias{registry_keygen}
\title{Create a signing key}
\usage{
registry_keygen(path, seed = NULL)
}
\arguments{
\item{path}{Where to write the secret key.}

\item{seed}{Optional 32 raw bytes to derive the key from, for tests that
need a fixed key. Omit for a real key.}
}
\value{
The public key, as \code{"ed25519:<hex>"}.
}
\description{
Generates an Ed25519 key pair, writes the secret half to \code{path}, and returns
the public half in the form \code{\link[=registry]{registry()}} takes. The seed comes from the
operating system's cryptographic random source, not from R's generator,
whose stream is reproducible by design.
}
\details{
The secret file is the one thing in this package that must not be published.
It is written with owner-only permissions where the platform has them, and
belongs outside the package source tree so that no build can sweep it up.
}
\examples{
file <- tempfile()
public <- registry_keygen(file)
substr(public, 1, 16)
unlink(file)
}
\seealso{
\code{\link[=registry_sign]{registry_sign()}}
}
