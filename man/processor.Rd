% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resource.R
\name{processor}
\alias{processor}
\title{Declare a post-download processor}
\usage{
processor(id, fn)
}
\arguments{
\item{id}{Short stable identifier for this transformation, for example
\code{"unzip"} or \code{"unzip-v2"}. Changing the transformation means changing
the id, which invalidates previously processed copies.}

\item{fn}{A function \verb{(input, output_dir)} returning a path inside
\code{output_dir}.}
}
\value{
An object of class \code{getaca_processor}.
}
\description{
A processor turns one verified path into another path: unpacking an
archive, or preparing a package-specific layout. It carries an \code{id} so the
processed result gets its own cache slot and its own provenance, rather
than being confused with the raw artefact it came from.
}
\details{
getaca knows nothing about file formats. It never reads data.
}
\examples{
unzipper <- processor("unzip", function(input, output_dir) {
  utils::unzip(input, exdir = output_dir)
  output_dir
})
}
