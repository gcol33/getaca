% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{getaca-progress}
\alias{getaca-progress}
\title{How a transfer reports itself}
\description{
getaca drives its own transfer loop, so what a download reports is a
decision the package makes rather than one libcurl makes for it. A reporter
receives an event for every transfer that begins, for the bytes that arrive
while it runs, and for how it ended, and renders them however it likes.
}
\details{
The events carry what getaca knows and a transfer library cannot: the
resource identity, which part of a series is moving, the size the registry
declares for it, and how much of it was already on disk when the attempt
resumed. That is why the declared total is available before the first byte
arrives, and why a series reports \verb{(part 2 of 3)} rather than three
unrelated downloads.
}
\section{Events}{

Each event is a list with a \code{type} and the fields for that type. A handler
switches on \code{type} and ignores what it does not use, so a later release
adding an event type leaves an existing reporter working.
\describe{
\item{\code{"begin"}}{A transfer attempt starts. \code{id} is the resource, which
\code{format()} renders including the part label where there is one. \code{url} is
the mirror. \code{total} is the declared size in bytes, or \code{NA} when the
declaration gives none. \code{offset} is what was already on disk, which is
non-zero when an interrupted transfer resumes.}
\item{\code{"bytes"}}{Bytes have arrived. \code{bytes} is the cumulative total for
this attempt including \code{offset}, so it can be compared against \code{total}
directly. Fires often; a reporter that draws is expected to throttle.}
\item{\code{"end"}}{The attempt finished. \code{status} is \code{"ok"} or \code{"failed"},
\code{reason} is the failure reason or \code{NA}, and \code{bytes} is what arrived.}
}

A reporter never affects the outcome of a retrieval. An error raised inside
one is caught, reported once as a warning, and the reporter is switched off
for the rest of the session's call rather than being allowed to fail a
download that is otherwise fine.
}

\seealso{
\code{\link[=getaca_progress]{getaca_progress()}} to choose one, \code{\link[=reporter]{reporter()}} to write one.
}
