% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{getaca_policy}
\alias{getaca_policy}
\title{Resolution policy and settings}
\usage{
getaca_policy(policy = NULL)
}
\arguments{
\item{policy}{One of \code{"bundled"}, \code{"current"}, \code{"pinned"}, \code{"offline"}, or
\code{NULL} to query without setting.}
}
\value{
When querying, the policy in effect. When setting, the previous
value of the \code{getaca.policy} option invisibly, \code{NULL} if it was unset.
}
\description{
\code{getaca_policy()} reports or sets the resolution policy for the current
session. The policy decides which registry state a name resolves through,
and is recorded in provenance so a result can always be traced back to it.
}
\section{Policies}{

\describe{
\item{\code{"bundled"}}{Always use the registry shipped with the declaring
package. The same installed package resolves the same bytes forever.
This is the default.}
\item{\code{"current"}}{Consult the author-controlled remote registry, falling
back to bundled when it cannot be reached. Lets an author repair a dead
mirror or publish a new version without a CRAN release.}
\item{\code{"pinned"}}{Resolve through a frozen local snapshot, so an analysis
keeps resolving what it resolved on the day it was written.}
\item{\code{"offline"}}{Never touch the network. Cached and bundled
information only.}
}

During \verb{R CMD check} resolution always collapses to \code{"offline"}, whatever
is set here.

Setting the policy sets the \code{getaca.policy} option and nothing else, and
returns what that option held before, so a caller that has to change it
can put it back:

\if{html}{\out{<div class="sourceCode">}}\preformatted{old <- getaca_policy("offline")
on.exit(options(getaca.policy = old), add = TRUE)
}\if{html}{\out{</div>}}
}

\examples{
getaca_policy()

# Setting it is reversible, because the previous value comes back.
old <- getaca_policy("offline")
getaca_policy()
options(getaca.policy = old)
}
