% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature.R
\name{registry_verify}
\alias{registry_verify}
\title{Verify a signed registry file}
\usage{
registry_verify(path, keys = NULL, now = Sys.time())
}
\arguments{
\item{path}{Path to a registry file.}

\item{keys}{Public keys to accept, as returned by \code{\link[=registry_keygen]{registry_keygen()}}.
Defaults to the keys the registry declares.}

\item{now}{Time to judge expiry against.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
Checks a registry against the detached signature beside it. Returns \code{TRUE}
or raises \code{getaca_error_signature} naming what failed.
}
\details{
This is the check resolution performs on a fetched remote registry, exposed
so an author can run it on their own output before publishing. In resolution
the trusted keys come from the bundled registry; here they default to the
keys the file itself declares, which answers whether a registry is
internally consistent rather than whether it is authentic.
}
\seealso{
\code{\link[=registry_sign]{registry_sign()}}
}
