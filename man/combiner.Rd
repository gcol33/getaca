% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resource.R
\name{combiner}
\alias{combiner}
\title{Declare how parts are combined}
\usage{
combiner(id, fn)
}
\arguments{
\item{id}{Short stable identifier for this transformation, for example
\code{"bsdiff"}.}

\item{fn}{A function \verb{(parts, output)}, where \code{parts} is a character vector
of verified local paths in declaration order and \code{output} is the file to
write. The return value is ignored.}
}
\value{
An object of class \code{getaca_combiner}.
}
\description{
A combiner turns the verified \code{\link[=part]{part()}}s of a resource, in declaration order,
into the single artefact the resource names. Concatenation is the default and
needs no declaration; a combiner is what a delta format calls for, since
applying a patch is knowledge about a file format and getaca has none.
}
\details{
The result is held to the resource's own SHA-256 like any other bytes, so a
combiner cannot produce something other than what the declaration promises.
That is also why the manifest records a combiner by \code{id} alone: the checksum
says the result is right, and the identifier only says what to run.
}
\examples{
combiner("bsdiff", function(parts, output) {
  # apply parts[-1] to parts[1], writing the result to output
  file.copy(parts[1], output)
})
}
\seealso{
\code{\link[=part]{part()}}
}
