% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{as_registry}
\alias{as_registry}
\title{Convert an authoring format into a registry}
\usage{
as_registry(x, package, ...)
}
\arguments{
\item{x}{A list, or a path to a \code{.yml}, \code{.yaml} or \code{.json} file.}

\item{package}{Declaring package name.}

\item{...}{Passed to \code{\link[=registry]{registry()}}.}
}
\value{
A \code{getaca_registry}.
}
\description{
Accepts the list shape a YAML or JSON registry parses into. Requires the
\code{yaml} or \code{jsonlite} package only when reading those formats; neither is a
hard dependency.
}
\details{
Multi-part records are expressible, since a \code{\link[=part]{part()}} is data. A \code{\link[=combiner]{combiner()}}
is not, so a record combined by anything other than the default has to be
declared with \code{\link[=resource]{resource()}} in R, where the function it names exists.
}
