% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature.R
\name{registry_sign}
\alias{registry_sign}
\title{Sign a registry file}
\usage{
registry_sign(path, key, expires = Sys.time() + SIGNATURE_DAYS * 86400)
}
\arguments{
\item{path}{Path to a registry file written by \code{\link[=registry_write]{registry_write()}}.}

\item{key}{Path to a secret key from \code{\link[=registry_keygen]{registry_keygen()}}.}

\item{expires}{When the signature stops being accepted. Re-sign an unchanged
registry to extend it. \code{NA} signs without an expiry, which leaves nothing
bounding how long a stale declaration is served.}
}
\value{
The signature path, invisibly.
}
\description{
Signs the declaration in a written registry, producing a detached signature
beside it. Sign after \code{\link[=registry_write]{registry_write()}}: writing is what stamps \code{created},
and the signature binds that stamp.
}
\details{
Publish the \code{.sig} alongside the registry it describes. getaca fetches it
from the registry's own URL with \code{.sig} appended.
}
\seealso{
\code{\link[=registry_keygen]{registry_keygen()}}, \code{\link[=registry_verify]{registry_verify()}}
}
