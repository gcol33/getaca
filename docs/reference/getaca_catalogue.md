# What is declared, and what is cached

What is declared, and what is cached

## Usage

``` r
getaca_catalogue(package = NULL)
```

## Arguments

- package:

  Restrict to one declaring package. `NULL` reports every package with
  cached resources.

## Value

A data frame, one row per resource, with cache state.
